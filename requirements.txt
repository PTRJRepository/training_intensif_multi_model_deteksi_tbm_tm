# Core ML/DL
ultralytics>=8.0.0
torch>=2.0.0
torchvision>=0.15.0
numpy>=1.24.0
opencv-python>=4.8.0

# Data processing
pandas>=2.0.0
pillow>=10.0.0
matplotlib>=3.7.0
scikit-learn>=1.3.0

# Geospatial (for Tools scripts - OPTIONAL, requires GDAL system library)
# Install separately if needed: pip install GDAL rasterio shapely fiona geopandas
# Note: shapely, pyproj, and rasterio are installed. 
# fiona and geopandas require GDAL C library on Windows.
rasterio>=1.3.0
shapely>=2.0.0
pyproj>=3.6.0
# fiona>=1.9.0  # Requires GDAL - install via conda or pre-compiled wheels
# geopandas>=0.13.0  # Requires fiona - install via conda

# Utilities
tqdm>=4.65.0
pyyaml>=6.0
seaborn>=0.12.0
